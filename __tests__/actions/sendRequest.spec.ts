import sendRequest from '../../src/actions/sendRequest'

describe('Actions: sendRequest', () => {
  it('should send request with correct method, headers and data', () => {

  })

  it('should run onSuccess', () => {
    
  })

  it('should run onError', () => {
    
  })
})
