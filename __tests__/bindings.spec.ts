import { replaceBindings } from '../src/bindings'

describe('Bindings', () => {
  it('should replace by full context', () => {

  })

  it('should replace by string representation of the context', () => {

  })

  it('should replace with values in multiple contexts', () => {
    
  })

  it('should use closest context if two contexts are declared with the same name', () => {
    
  })

  it('should replace with value in context (object)', () => {

  })

  it('should replace with value in context (array)', () => {

  })

  it('should replace with value in context (mix between array and object)', () => {

  })

  it('should not replace if path is invalid', () => {

  })

  it('should not replace if no context is found', () => {

  })

  it('should not replace if path doesn\'t exist in the referred context', () => {

  })

  it(
    'should not replace if referred context exists but is not in the current scope (herarchy)',
    () => {

    },
  )

  it('should handle expression (binding) scaping correctly', () => {

  })
})
